# ========================
# MongoDB Connection
# ========================
# Replace <username> and <password> with your MongoDB Atlas credentials.
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/hackathon_platform

# ========================
# JWT Authentication
# ========================
# Secret key for signing JWT tokens (use a strong random value in production).
JWT_SECRET=changeme

# ========================
# Admin Credentials
# ========================
# These are the credentials for logging into the admin panel (/admin).
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123

# ========================
# App Config
# ========================
# Base URL for the frontend app.
NEXT_PUBLIC_BASE_URL=http://localhost:3000
